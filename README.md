# whisper-investigation
Set of notebooks and backend service to run pure whisper model.

Repo contains python backend service that is responsible to run online inference. Python service constructed with the usage of well known python stack. Also it contains several notebooks that help to setup Sagemaker Endpoint.
